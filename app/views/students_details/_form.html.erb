<%= form_with(model: students_detail) do |form| %>
  <% if students_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(students_detail.errors.count, "error") %> prohibited this students_detail from being saved:</h2>

      <ul>
        <% students_detail.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.text_field :age %>
  </div>

  <div class="field">
    <%= form.label :gender %>
    <%= form.text_field :gender %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
