<%= form_with(model: student_basicdetail) do |form| %>
  <% if student_basicdetail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student_basicdetail.errors.count, "error") %> prohibited this student_basicdetail from being saved:</h2>

      <ul>
        <% student_basicdetail.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
